-- =============================================================
-- 1to1: 外部で調整した日時・会議URLを手動で記録できるようにする
-- =============================================================
ALTER TABLE one_on_one_sessions ADD COLUMN manual_conference_url TEXT;
